# 3D models

Place **`sample_exported.ply`** here for the INTERACTIVE 3D default scene.

That file is tracked with **Git LFS** (see repo root `.gitattributes` and `docs/GIT_LFS_SAMPLE_SCENE.md`). Do not commit multi‑hundred‑MB binaries as normal Git blobs.
